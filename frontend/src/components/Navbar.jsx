export default function Navbar({onNavigate}){const links=['about','skills','experience','projects','education','contact'];return <header className="site-header"><nav className="nav container" aria-label="Primary"><a className="brand" href="#home" onClick={event=>{if(onNavigate){event.preventDefault();onNavigate(0)}}}>NK.</a><div className="nav-links">{links.map((label,index)=><a key={label} href={`#${label}`} onClick={event=>{if(onNavigate){event.preventDefault();onNavigate(index+1)}}}>{label}</a>)}</div></nav></header>}
