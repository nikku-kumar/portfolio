import {fireEvent,render,screen} from '@testing-library/react'; import {expect,test,vi} from 'vitest'; import ContactForm from './ContactForm';
test('submits contact details and shows confirmation',async()=>{global.fetch=vi.fn(()=>Promise.resolve({ok:true,json:()=>Promise.resolve({message:'Thanks! Your message has been received.'})}));render(<ContactForm/>);fireEvent.change(screen.getByLabelText('Name'),{target:{value:'Recruiter'}});fireEvent.change(screen.getByLabelText('Email'),{target:{value:'r@example.com'}});fireEvent.change(screen.getByLabelText('Subject'),{target:{value:'Role'}});fireEvent.change(screen.getByLabelText('Message'),{target:{value:'A suitable backend opportunity'}});fireEvent.click(screen.getByRole('button',{name:'Send message'}));expect(await screen.findByText(/message has been received/i)).toBeInTheDocument();});
