export default function Projects({items}){
  return <section id="projects" className="section projects-section" data-reveal><div className="container">
    <div className="section-heading split-heading"><div><p className="eyebrow"><span>04</span> Selected work</p><h2>Selected systems, built with <em>a point of view.</em></h2></div><p>Projects presented through the engineering decisions they demonstrate—not just a list of technologies.</p></div>
    <div className="project-list">{items.map(project=><article className="project-card" key={project.name}><div className="project-number">PROJECT / {project.number}</div><div className="project-title"><h3>{project.name}</h3><p>{project.summary}</p></div><dl className="project-details"><div><dt>Problem</dt><dd>{project.problem}</dd></div><div><dt>Architecture</dt><dd>{project.architecture}</dd></div><div><dt>Impact</dt><dd>{project.impact}</dd></div></dl><div className="project-footer"><div className="tags" aria-label={`${project.name} technologies`}>{project.technologies.map(technology=><span key={technology}>{technology}</span>)}</div>{(project.github||project.live)&&<div className="project-actions">{project.github&&<a href={project.github} target="_blank" rel="noreferrer">Source <span aria-hidden="true">↗</span></a>}{project.live&&<a href={project.live} target="_blank" rel="noreferrer">Live demo <span aria-hidden="true">↗</span></a>}</div>}</div></article>)}</div>
  </div></section>;
}
