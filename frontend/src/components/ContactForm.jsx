import {useState} from 'react'; import {sendContact} from '../api/portfolioApi';
const initial={name:'',email:'',subject:'',message:''};
export default function ContactForm(){const [form,setForm]=useState(initial);const [status,setStatus]=useState('');const [busy,setBusy]=useState(false);const change=e=>setForm({...form,[e.target.name]:e.target.value});const submit=async e=>{e.preventDefault();setBusy(true);setStatus('');try{const data=await sendContact(form);setStatus(data.message);setForm(initial);}catch(err){setStatus(err.message);}finally{setBusy(false)}};return <section id="contact" className="section container contact"><div><p className="eyebrow">Get in touch</p><h2>Let’s build something reliable.</h2><p>Have a Java backend role or a project in mind? Send me a message.</p></div><form onSubmit={submit}>{['name','email','subject'].map(field=><label key={field}>{field[0].toUpperCase()+field.slice(1)}<input name={field} type={field==='email'?'email':'text'} value={form[field]} onChange={change} required maxLength={field==='subject'?150:100}/></label>)}<label>Message<textarea name="message" value={form.message} onChange={change} required minLength="10" maxLength="2000" rows="5"/></label><button className="button primary" disabled={busy}>{busy?'Sending…':'Send message'}</button>{status&&<p className="form-status" role="status">{status}</p>}</form></section>}
