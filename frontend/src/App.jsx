import {useEffect,useState} from 'react'; import {getPortfolio} from './api/portfolioApi';
import Navbar from './components/Navbar'; import Hero from './components/Hero'; import About from './components/About'; import Skills from './components/Skills'; import ExperienceTimeline from './components/ExperienceTimeline'; import Projects from './components/Projects'; import Education from './components/Education'; import ContactForm from './components/ContactForm'; import Footer from './components/Footer';
export default function App(){const [data,setData]=useState(null);const [error,setError]=useState('');useEffect(()=>{getPortfolio().then(setData).catch(e=>setError(e.message))},[]);if(error)return <main className="state"><h1>Portfolio unavailable</h1><p>{error}</p><p>Start the Spring Boot API on port 8080 and refresh.</p></main>;if(!data)return <main className="state"><p>Loading portfolio…</p></main>;return <><Navbar/><main><Hero profile={data.profile}/><About profile={data.profile}/><Skills groups={data.skills}/><ExperienceTimeline items={data.experience}/><Projects items={data.projects}/><Education items={data.education}/><ContactForm/></main><Footer profile={data.profile}/></>}
